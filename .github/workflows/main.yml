name: Deploy to Local

on:
  push:
    branches:
      - main 

jobs:
  deploy:
    runs-on: DESKTOP-PB534D3

    steps:
      - name: Checkout Repository
        uses: actions/checkout@v2

      


      - name: Deploy to Local
        run: |
          cp -r $GITHUB_WORKSPACE/* D:\WEEK4

